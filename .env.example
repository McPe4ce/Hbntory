# Copy to .env and fill in. .env is gitignored.

# Name of the Product API image provided for the project.
# Only needed for: docker compose --profile real up
PRODUCT_API_IMAGE=

# Signs the backoffice JWT cookies. Change it for anything other than local dev.
SECRET_KEY=dev-only-change-me
